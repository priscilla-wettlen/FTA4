package textEditors.model;

public class Modifier extends Thread
{
    /* adust to your project
    SharedBuffer buffer;
    public Modifier(SharedBuffer buffer) {
        this.buffer = buffer;
    }
*/
    @Override
    public void run() {
/*
        while (!buffer.continuModify())
        {
            try {
                Thread.sleep(50);
                buffer.modifyData();
            } catch (InterruptedException e) {
                throw new RuntimeException(e);
            }
        }*/
    }
}
